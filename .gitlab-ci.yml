include:
  - autodevops-static.yml

# include:
#   - project: SocialGouv/gitlab-ci-yml
#     file: /autodevops-static.yml
#     ref: v17.1.1

variables:
  AUTO_DEVOPS_RELEASE_AUTO: "🔖"
  AUTO_DEVOPS_PRODUCTION_AUTO: "🚀"

Lint:
  rules:
    - when: never

Test:
  rules:
    - when: never

Install:
  extends: .next_static_build

Register image:
  extends: .register_app
